export const initialState = {
    token: '',
    commentList: [],
    savedComments: [],
    userInfo: null,
    isFetching: false
}

export const appReducer = (state = initialState, action) => {
    switch (action.type) {

        default:
            return state
    }
}